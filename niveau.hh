#pragma once 
#include <iostream>
#include <string>
#include <map>
#include <vector>
#include <list>
#include <map>

using namespace std; 

class Niveau{
private:
	int _id;
public:
	Niveau(int x):_id(x){};
	map<int, int> Init();



};
